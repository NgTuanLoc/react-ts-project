import styled from 'styled-components';

import { SearchBar } from '../components';

const Home = () => {
	return (
		<Container>
			<SearchBar />
		</Container>
	);
};

const Container = styled.section``;

export default Home;
